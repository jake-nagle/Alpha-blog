<h2 align = 'center'> ALPHA BLOG </h2>
<div class = 'well col-xs-8 col-xs-offset-2'>
  <h4 class = 'description'><strong> <%= @article.title %> </strong></h4>
  <hr>
    <%= simple_format(@article.description) %>
  <div class = 'article-actions'>
    <%= link_to 'Articles listing', articles_path, class: 'btn btn-xs btn-success' %>
    <%= link_to 'Edit this article', edit_article_path(@article), class: 'btn btn-xs btn-primary' %>
    <%= link_to 'Delete this article', article_path(@article), method: :delete,
                data: {confirm: 'Delete article?'}, class: 'btn btn-xs btn-danger' %>
  </div>
</div>